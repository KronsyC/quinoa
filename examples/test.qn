import @io;
import @lang::assertions as assert;

module Test is Entry{
    pub func main(argc:int, argv:c_str*)->int{
        assert::equals(argc, 2);
        assert::truthy(1);
        return 0;
    }

}
