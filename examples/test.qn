import @io;


module Test is Entry{
    pub func main(argc:int, argv:c_str*)->int{
        greet("Bob");
        return 0;
    }

    func greet(name:c_str){
        io::print("Hello, ");
        io::println(name);
    }
}
