import @io;
import @lang::instantiable as Instantiable;

module Person is Instantiable{
    inst let age:int;
    inst let name:c_str;
    inst let surname:c_str;
    
}

module Test is Entry{


    pub func main(argc:int, argv:c_str*)->int{
        let emp : @Person;
        emp.age = 27;
        emp.name = "John";
        emp.surname = "Doe";

        io::print("Employee: ");
        io::print(emp.name);
        io::print(" ");
        io::println(emp.surname);
        return emp.age;
    }

}


