import @io;

seed Pet{
    pub inst func make_noise();
}

module Cat is Pet{
    pub inst func make_noise(){
        io::println("Meow?");
    }
}

module Main is Entry{
    pub func main(argc: int, argv:c_str*) -> int{
        return 0;
    }

    func noise(pet : Pet){
        pet.make_noise();
    }
}