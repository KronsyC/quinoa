import @io;
import @syscall;

module Assert is Exported{
    func equals<T>(v1: T, v2:T){
        if(v1 != v2){
            io::println("Error: Equality Assertion Failed");
            syscall::exit(20);
        }
    }
}