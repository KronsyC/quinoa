import @syscall;
module IO is Exported{

    func strlen(str:string)->i64{
        let i:i64 = 0;
        while(str[i] != 0){
            i=i+1;
        }
        return i;
    }

    func println(message:string) -> i64{
        return syscall::call(1, 1, message, strlen(message));
    }
}