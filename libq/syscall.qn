
module Syscall is Exported{


	// 0
	#[ link_extern "read" ]
	func read( fd: u32, ptr:u64, len:u64)->i64;

	// 1
	#[ link_extern "write" ]
	func write(fd : u32, ptr : u64, len : u64) ->i64;

	// 2
	#[ link_extern "open" ]
	func open(filename:u64, flags:int, mode:int)->i64;

	// 3
	#[ link_extern "close" ]
	func close(fd:u32)->i64;

	#[ link_extern "exit" ]
	func exit(status: u8 )->i64;
}
