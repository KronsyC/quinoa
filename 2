import @memory;
import @io;
module Test is Entry{

    type Test = struct{
      arr : char[];
    };
  
    func main() -> i32{
      let alloc = memory::alloc::<char>(10);
      let me = Test{arr : alloc;};
      io::println(me.arr);
      return numbers.allocation[10];
    }
    
}
